import React from 'react';
import {StyleSheet, View, TouchableOpacity, Text} from 'react-native';

const AttributeAdder = () => {}

const styles = StyleSheet.create({})

export default AttributeAdder;